# รหัสผ่านใช้ตัวอักษรอังกฤษ/ตัวเลขเท่านั้น (ไฟล์ .properties อ่านภาษาไทยไม่ได้)
# คัดลอกไฟล์นี้เป็น keystore.properties แล้วใส่ค่าจริง (keystore.properties จะไม่ถูก commit)
# เก็บไฟล์ .jks ไว้นอกโปรเจกต์ (และสำรองไว้หลายที่) — ใช้ / แทน \ ใน path
storeFile=C:/Users/Kapom/keys/ping-pong-counter-release.jks
storePassword=CHANGE_ME
keyAlias=ping-pong-counter
keyPassword=CHANGE_ME
